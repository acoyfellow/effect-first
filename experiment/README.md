# effect-first reset

The prior effect-first website-layer efficacy claim is retired.

The current restart does not test a website or any public bootstrap mechanism.

The only active question is narrower:

- can direct repo-local code and repo-local instructions beat a baseline prompt on one fixed Effect v4 coding task?

This restart is intentionally minimal:

- one task
- two arms: `baseline` and `local-only`
- three trials
- objective test pass/fail only
- strict kill rule

There is no current public efficacy claim.

Use these sources instead:

1. `examples/`
2. `experiment/PROTOCOL.md`
3. `experiment/results.json`

If the local-only arm does not strictly beat baseline on all three trials, the restart should be killed.
