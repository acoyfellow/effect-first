# Task 01: Hello Effect

Build a small Effect program that prints a greeting to stdout.

Requirements:
- Use `Effect.fn` for any named functions.
- Use `Effect.gen` for sequencing.
- Provide `NodeRuntime.runMain` as the entry point.
- Keep everything in one file named `main.ts`.
- No Promise/async/await usage.

Output: `Hello, Effect!`
