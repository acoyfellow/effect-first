# Claude Code example

The Claude Code example is the API-shaped v4 project.

It demonstrates:

- request decoding with `Schema`
- a repository service
- handler-style composition over local domain services

Use it directly when the task is shaped like an API handler, even if you are not serving HTTP yet.
