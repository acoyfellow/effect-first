# Cursor example

The Cursor example is the schema-heavy validation project.

It demonstrates:

- a validation-focused service
- schema decoding of unknown input
- a registry-style boundary for validation logic

Use it directly when the task is primarily about input validation.
