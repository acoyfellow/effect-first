---
description: effect-first local-only bootstrap
alwaysApply: true
---

The website-layer experiment is retired. Do not use `kit/` or any web route for this project.

From the repo root:

1. `examples/cursor/README.md`
2. `examples/cursor/.cursor/rules/effect-first.mdc`
3. `examples/cursor/src/errors.ts`
4. `examples/cursor/src/schema.ts`
5. `examples/cursor/src/registry.ts`
6. `examples/cursor/src/validator.ts`

Then use this example for schema-first validation flows.
