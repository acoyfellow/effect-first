# Codex example

The Codex example shows multi-service composition in Effect v4.

It combines:

- a greeter service
- a todo repository service
- shared provision through `Layer.mergeAll`

Use this example directly when you need more than one service in the same program.
