# Local-Only Agent Instructions (Pi)

The website-layer experiment is retired. Do not use any retired bootstrap layer or web route for this example.

Read this example directly:

1. `examples/pi/README.md`
2. `examples/pi/AGENTS.md`
3. `examples/pi/src/errors.ts`
4. `examples/pi/src/schema.ts`
5. `examples/pi/src/health-checker.ts`
6. `examples/pi/src/health-checker.test.ts`

Then use this example for retries, timeouts, and `ManagedRuntime`.
